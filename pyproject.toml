[project]
name = "house-price-prediction"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "pandas",
    "numpy",
    "matplotlib",
    "seaborn",
    "scikit-learn",
    "joblib",
    "fastapi",
    "uvicorn",
]

[tool.vercel]
entrypoint = "backend.main:app"